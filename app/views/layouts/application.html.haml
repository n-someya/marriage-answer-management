!!!
%html
  %head
    %title 解答管理コンソール＠二次会
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags

  %body

    %nav.navbar.navbar-default{:role => "navigation"}
      .container-fluid
        .navbar-header
          %button.navbar-toggle{"data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
            %span.sr-only Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %a.navbar-brand{:href => '/'} 解答管理コンソール＠二次会

        #bs-example-navbar-collapse-1.collapse.navbar-collapse
          %ul.nav.navbar-nav.navbar-right
            - if current_admin
              %li
                = link_to root_path, style: 'padding-right: 10px;' do
                  = image_tag current_admin.gravatar_url(length: 20), width: 20, height: 20
                  = current_admin.email
              %li.dropdown
                = link_to '#', class: 'dropdown-toggle', 'data-toggle' => 'dropdown', style: 'padding-left: 10px;' do
                  %span.caret
                %ul.dropdown-menu
                  -#%li= link_to 'プロフィール設定', edit_home_settings_profiles_path
                  -#%li.divider
                  %li= link_to 'ログアウト', destroy_admin_session_path, method: :delete
            - else
              %li= link_to '始める', new_admin_registration_path
              %li= link_to 'ログイン', new_admin_session_path

    -#.container
      -#= render partial: 'shared/breadcrumbs'

    -#.container
      -#- devise_alert_mapping = {'alert' => :danger, 'notice' => :info}
      -#- flash.each do |type, msg|
        -#- if type.in?(devise_alert_mapping.keys)
          -#%div{class: "alert alert-#{devise_alert_mapping[type]}"}= msg
        -#- else
          -#%div{class: "alert alert-#{type}"}= msg

    %div{class: content_for?(:container_class) ? yield(:container_class) : 'container'}
      = yield

    %footer.text-center
      %div
        Happy Wedding, jun and yukari 

